org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# IntelliJ is not fully compatible with the configuration cache yet (fabric-loom#1349)
org.gradle.configuration-cache=false

# Minecraft is unobfuscated from 26.1, so there is no mappings property: the source
# is on Mojang names and loom no longer remaps.
minecraft_version=26.3
loader_version=0.19.5
loom_version=1.17-SNAPSHOT
fabric_api_version=0.160.7+26.3

mod_version=2.0.0
maven_group=com.finndog
archives_base_name=structure-editor
